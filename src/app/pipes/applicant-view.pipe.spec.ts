import { ApplicantViewPipe } from './applicant-view.pipe';

describe('ApplicantViewPipe', () => {
  it('create an instance', () => {
    const pipe = new ApplicantViewPipe();
    expect(pipe).toBeTruthy();
  });
});
